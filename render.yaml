services:
  - type: web
    name: studychat
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -k eventlet -w 1 --bind 0.0.0.0:$PORT app:socketio_app
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: RENDER
        value: "true"